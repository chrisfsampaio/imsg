#!/usr/bin/env ruby
require 'imsg'

# Control+C trick in order to get out of exit() signal gracefully
trap("SIGINT") { throw :ctrl_c }
catch :ctrl_c do
  begin
    Imsg::CLI.start(ARGV)
  rescue Exception
  end
end
